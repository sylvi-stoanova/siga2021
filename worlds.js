(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)'),menu=document.querySelector('.menu-toggle'),nav=document.querySelector('#navigation'),dialog=document.querySelector('dialog');const closeMenu=()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label',document.body.dataset.menuOpen)};menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';nav.classList.toggle('open',open);menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',document.body.dataset[open?'menuClose':'menuOpen'])});nav.addEventListener('click',e=>{if(e.target.closest('a'))closeMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){closeMenu();menu.focus()}});document.querySelectorAll('[data-open-concept]').forEach(b=>b.addEventListener('click',()=>{dialog.showModal();document.body.classList.add('dialog-open')}));document.querySelectorAll('.dialog-close,.dialog-done').forEach(b=>b.addEventListener('click',()=>dialog.close()));dialog.addEventListener('close',()=>document.body.classList.remove('dialog-open'));const steps=[...document.querySelectorAll('[data-step]')],images=[...document.querySelectorAll('[data-industry-image]')];let pending=false;const tick=()=>{pending=false;let nearest=0,distance=Infinity;steps.forEach((s,i)=>{const r=s.getBoundingClientRect(),d=Math.abs(r.top+r.height/2-innerHeight*.58);if(d<distance){nearest=i;distance=d}});steps.forEach((s,i)=>s.classList.toggle('active',i===nearest));images.forEach((s,i)=>s.classList.toggle('active',i===nearest));};addEventListener('scroll',()=>{if(!pending){pending=true;requestAnimationFrame(tick)}},{passive:true});addEventListener('resize',tick);tick();document.querySelectorAll('.world').forEach(p=>{if(!matchMedia('(hover:hover) and (pointer:fine)').matches)return;p.addEventListener('pointermove',e=>{if(reduce.matches)return;const r=p.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;p.style.rotate=`${-y} ${x} 0 ${Math.min(3,Math.hypot(x,y)*5)}deg`;});p.addEventListener('pointerleave',()=>p.style.rotate='none')});})();
