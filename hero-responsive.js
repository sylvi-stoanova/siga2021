(()=>{'use strict';const hero=document.querySelector('body.c .hero>.hero-copy');if(!hero)return;const fine=matchMedia('(hover:hover) and (pointer:fine)'),reduce=matchMedia('(prefers-reduced-motion: reduce)');let raf=0,x=0,y=0;const reset=()=>{cancelAnimationFrame(raf);raf=0;hero.removeAttribute('data-spotlight');};hero.addEventListener('pointermove',e=>{if(!fine.matches||reduce.matches||e.pointerType==='touch')return;const r=hero.getBoundingClientRect();x=e.clientX-r.left;y=e.clientY-r.top;if(!raf)raf=requestAnimationFrame(()=>{raf=0;hero.style.setProperty('--hero-x',x+'px');hero.style.setProperty('--hero-y',y+'px');hero.setAttribute('data-spotlight','');});},{passive:true});hero.addEventListener('pointerleave',reset);fine.addEventListener('change',reset);reduce.addEventListener('change',reset);addEventListener('resize',reset,{passive:true});})();
